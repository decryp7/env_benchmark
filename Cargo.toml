[package]
name = "env_benchmark"
version = "0.1.0"
edition = "2021"

[dependencies]
console = "0.15.8"
indicatif = "0.17.8"
parse-size = "1.0.0"
sysinfo = "0.31.2"

[profile.dev]
incremental = true # Compile your binary in smaller steps.

[profile.release]
opt-level = 0
