[package]
name = "env_benchmark"
version = "0.1.0"
edition = "2021"

[dependencies]
console = "0.15.8"
indicatif = "0.17.8"
sysinfo = "0.31.2"
thousands = "0.2.0"

[profile.dev]
incremental = true # Compile your binary in smaller steps.

[profile.release]
opt-level = 0
